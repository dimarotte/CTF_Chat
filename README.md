# CTF_Chat

This project is a CTF where Bob and Alice exchange secret messages through a chat application — and your job is to intercept them.
Each challenge introduces a new vulnerability, a bit harder than the last. Everything runs locally in Docker, the stack is Flask and MySQL. No prior CTF experience required.
